services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - LOG_LEVEL=debug
      - ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8000
    volumes:
      - ./src:/app/src
